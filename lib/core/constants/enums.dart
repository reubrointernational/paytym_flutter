enum CalendarTabs { meeting, schedule }

enum ChatType { hod, employee }

enum Payments { payment, advance, logout }

enum Reports { payslip, deduction, attendance }

enum LeavesTab { all, casual, sick }
